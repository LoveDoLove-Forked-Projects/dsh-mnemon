---
"dsh-mnemon": patch
---

Hide the owning conversation's width handles while the Builtin memory page is displayed so they cannot appear or intercept input over its content. Returning to Chat restores normal width dragging; the resident composer, Sidebar and other plugin views remain unchanged.
