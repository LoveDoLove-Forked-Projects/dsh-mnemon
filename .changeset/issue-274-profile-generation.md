---
"dsh-mnemon": patch
---

Load the live settings schema only when the host provides the public Volatile APIs, retaining ordinary settings on older Desktop generation installs. Preserve the Starter group, component switches and existing settings namespaces.

仅在宿主提供公开 Volatile API 时导入动态设置 schema，让旧版 Desktop generation 安装继续使用普通设置。保留 Starter 分组、组件开关与已有设置命名空间。
