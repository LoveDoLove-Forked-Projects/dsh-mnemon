---
"dsh-mnemon-source-runtime": patch
---

Hide the Windows console for Runtime Git branch probes so switching conversations in GUI hosts does not flash a terminal window. Preserve branch filtering and the existing fallback when Git cannot resolve a branch.
